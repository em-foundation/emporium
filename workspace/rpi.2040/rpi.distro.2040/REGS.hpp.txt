#ifndef __REGS_M
#define __REGS_M

#include "../../rpi.2040/rpi.distro.2040/inc/rp2040.h"

#define IO_BANK0_CTRL_get(pid) (((uint32_t)(IO_BANK0_BASE)) + (pid * 8) + 4)
#define IO_BANK0_INTR_get(pid) (((uint32_t)(&IO_BANK0->INTR0)) + ((pid >> 3) * 4))

#define PADS_BANK0_GPIO_get(pid) (((uint32_t)(&PADS_BANK0->GPIO0)) + (pid * 4))

#endif
